> Generated by /vibe:docs — manual edits will be overwritten; use README for hand-written content.

# Testing

## Running the suite

```sh
go test ./...
```

Lint (run alongside tests, part of this project's definition of done):

```sh
gofmt -l .
go vet ./...
```

## Conventions

- Tests are co-located with source: `*_test.go` alongside the file it
  tests (e.g. `air/parser_test.go` next to `air/parser.go`), one test file
  per source file.
- Test names describe behavior, not implementation:
  `TestParse_MultiActionSample_ProducesExpectedAnimations`, not
  `TestParse`.
- Every unit of behavior is covered by: the nominal path, at least two
  edge cases, and an error/invalid-input path.
- Round-trip tests (serialize → parse → same result) are required for any
  write-path code (`.air`/`.cns`). `air.Serialize` has this today, checked
  against `air.Parse` (semantic round trip: equivalent `Animation`/`Frame`
  data, not byte-exact text). `air.Document` has a separate, byte-exact
  round-trip suite: parsing a realistic fixture and serializing it back
  reproduces the file exactly, comments included, plus edge cases (no
  trailing newline, comment/blank-only file, CRLF line endings) and error
  paths (a failing reader, malformed content). `sff.SerializeV1`/
  `sff.EncodePCX` have the same semantic round-trip treatment: serialize,
  re-parse with `ParseV1`, re-decode with `DecodePCX`, and compare metadata
  and pixel buffers (not raw bytes, since `.sff` round-trip fidelity is
  defined semantically — see
  [`.vibe/decisions/005-sff-v1-serialize-is-semantic-not-byte-exact-round-trip.md`](../.vibe/decisions/005-sff-v1-serialize-is-semantic-not-byte-exact-round-trip.md)).
  `sff.SerializeV2`/`sff.EncodeV2Sprite` have the same semantic round-trip
  treatment against `ParseV2`/`DecodeV2Sprite`, including palette bank
  metadata and color data, not just sprite pixels. `def.Serialize` has the
  same semantic round-trip treatment, checked against `def.Parse`.
  `def.Document` has a separate, byte-exact round-trip suite, mirroring
  `air.Document`: parsing a realistic fixture and serializing it back
  reproduces the file exactly, comments, section ordering, and unrecognized
  sections included. `cns.Serialize` has the same semantic round-trip
  treatment, checked against `cns.Parse` (including a controller with no
  triggers and a `Statedef` with no controllers). `cns.Document` has a
  separate, byte-exact round-trip suite, mirroring `air.Document`/
  `def.Document`: parsing the realistic fixture (`cns/testdata/sample.cns`)
  and serializing it back reproduces the file exactly, comments, block
  ordering, and the unrecognized `[Data]` section included.

## What's covered today

| Package | What the tests check |
|---|---|
| `character` | The root `Character` struct's zero value; assembling `Animations`/`Sprites` from a real `.air` fixture (`air/testdata/sample.air`, via `air.Parse`) and a matching hand-built sprite collection, checking `ResolveSprite` returns the correct `sff.Sprite` (against distinguishing metadata, not just non-zero output) for every frame of a multi-frame animation; edge cases: `ResolveSprite` returning a descriptive error for a frame referencing a sprite absent from the loaded sprites, and the same on a zero-value `Character` (no sprites loaded at all). `Load` against a real, on-disk `.def`/`.air`/`.sff`/`.cns` fixture folder it builds itself (`.sff` via `sff.SerializeV1`), checking the returned `Character`'s name, animations, sprites, and `StateDefs` (state number, move type, and controller) are correct and that every loaded frame resolves against the loaded sprites via `ResolveSprite`; error paths: the referenced `.air` file missing, the referenced `.sff` file missing, the referenced `.cns` file missing, and the `.def` file itself missing — each returning a descriptive error rather than panicking |
| `character/air` | `SpriteResolver`: every `Frame` in a multi-frame, multi-group animation resolving to its correct `sff.Sprite` (checked against distinguishing metadata, not just non-zero output); an ambiguous-looking `(group, image)` pair (a group/image value each individually present elsewhere) correctly failing rather than cross-matching; a resolver built from `nil`/empty sprite groups; a resolver working the same against sprites carrying v2-only concepts (a non-zero palette bank index) to document version-independence; error path: a descriptive error naming the missing `(group, image)` for a frame with no matching sprite. `Animation`/`Frame`/`ClsnBox` zero values and field assignment; `Parse` against a multi-action `.air` sample (action numbers, frame sequences, `Clsn1Default`/`Clsn2Default` resolution, one-shot `Clsn1:`/`Clsn2:` overrides, the `Loopstart` marker, optional flip/blend fields), an action with no frames, multiple default Clsn boxes; edge cases and error paths: empty and comment-only input, whole-line/trailing comments mixed with valid data, a malformed action header (as the first line and mid-file), missing/non-numeric frame fields, negative group/image indices (rejected) vs. negative X/Y/time (accepted), and a failing reader; `Serialize` round-tripped through `Parse` for a multi-action animation, per-frame Clsn boxes, and the `Loopstart` marker (mid-animation and pointing past the last frame); edge cases and error paths: optional Flip/Blend fields omitted when unset, an empty animation list producing empty output, and a failing writer returning an error instead of panicking; `ParseDocument`/`Document.Serialize` round-tripped byte-for-byte against a realistic multi-action fixture (`air/testdata/sample.air`), asserting both the decoded `Animations` (no lost frames/Clsn boxes/`Loopstart`) and that every comment in the fixture survives in the output; edge cases: a file with no trailing newline, a comment/blank-only file, CRLF line endings, and mutating `Animations` after parsing not affecting `Serialize`'s output; error paths: a failing reader, malformed content, and a failing writer |
| `character/def` | `CharacterInfo` zero value (all string fields empty, nil `StateFiles`/`Palettes` safe to range over) and field assignment (all fields, including multi-entry `StateFiles`/`Palettes`, preserve their assigned order); edge cases: two `CharacterInfo` instances built with their own slice literals don't share underlying `StateFiles`/`Palettes` storage, and a minimal instance (only `Name`/`SpriteFile` set) leaves every other field at its zero value rather than requiring a placeholder. `Parse` against a multi-section `.def` sample (name, author, every `[Files]` reference, `StateFiles` in file-appearance order, `Palettes` sorted by number despite appearing out of order in the source); edge cases and error paths: an unrecognized section (`[Arcade]`) skipped without aborting the sections around it, case-insensitive section names/keys with both quoted and unquoted values, comment lines and blank lines ignored, an unrecognized key inside a known section ignored, empty input returning a zero-value `CharacterInfo`, a malformed `key=value` line and a malformed section header each reporting the offending line number, and a failing reader returning a wrapped error |
| `character/cns` | `StateDef`/`Controller` zero values (all fields zero, nil `Controllers`/`Triggers`/`Parameters` safe to range over and to look up on) and field assignment (a `StateDef` with multiple controllers, and a controller with no `Triggers` documented as running unconditionally, not "never"). `Parse` against a realistic multi-Statedef `.cns` fixture (`cns/testdata/sample.cns`, covering `Statedef`/`State` header parameters, multiple controllers per state, triggers spread across `trigger1`/`trigger2`/`triggerall` keys in file order, and parameter values preserved verbatim including embedded commas); edge cases: empty input returning an empty result, an unrecognized section (e.g. `[Data]`) between two `Statedef` blocks skipped without aborting, a `Statedef` with zero controllers, and a controller with no trigger lines; error paths: a `[...]` header missing its closing `]`, a malformed `Statedef`/`State` header (non-numeric or missing state number), a `[State N]` block with no enclosing `Statedef`, a malformed `key=value` line, an invalid integer value for a header field (`anim`), an invalid boolean value for a header field (`ctrl`), and a failing reader. `Serialize` checked against an exact expected-text assertion for a single `Statedef` with one controller, then round-tripped through `Parse` for a multi-`Statedef` set covering a negative state number, a `Statedef` with zero controllers, and an unconditional controller (no triggers); edge cases and error paths: a `Statedef` with zero controllers omits any `[State N]` section, a nil input produces empty output, and a failing writer returns a wrapped error instead of panicking. `ParseDocument`/`Document.Serialize` round-tripped byte-for-byte against the realistic fixture (`cns/testdata/sample.cns`), asserting both the decoded `StateDefs` and that every comment and the unrecognized `[Data]` section survive in the output; edge cases: empty input, and mutating `StateDefs` after parsing not affecting `Serialize`'s output; error paths: a failing reader and malformed content |
| `character/sff` | `Sprite`/`SpriteGroup` zero values (all fields zero, nil `Sprites` safe to range over) and field assignment (multiple sprites in a group keep their values and order); edge case: `SpriteGroup` does not itself enforce that every `Sprite.Group` matches its own `Index` — a plain-data container, not a validated one; `ParseV1` against synthetic (byte-built) `.sff` v1 fixtures: header fields (version, group/image counts, palette-sharing flag), the sprite index table resolving `(group, image)` pairs to file offsets via `Offset`, a zero-sprite file, and a linked sprite (zero pixel-data length, `LinkedIndex` set); error paths: a truncated header, a wrong signature, a sprite subfile chain that ends before the header's declared image count, and a subheader offset pointing past the end of the file; `DecodePCX` against synthetic (byte-built) PCX fixtures: mixed run-length and literal scanline bytes, a run split across the maximum single-run length so it lands exactly on a scanline boundary (no cross-row bleeding), and a pixel value that itself looks like a run marker correctly escaped as a length-1 run; error paths: data too short for the PCX header, an unsupported encoding or bit depth, invalid image bounds, a run-length unit missing its value byte at end of data, data ending mid-scanline, and a run overflowing its scanline; `EncodePCX` against hand-computed expected output bytes (not just its own decoder, to avoid a self-referential check): mixed runs and literals, a run over 63 pixels split across two run units, and an odd width padded to an even `bytesPerLine`, plus an `EncodePCX`→`DecodePCX` integration check; error paths: a pixel buffer length that doesn't match `Width*Height`, and non-positive dimensions; `SerializeV1` round-tripped through `ParseV1`/`DecodePCX` for a multi-sprite, multi-group fixture (metadata and pixel data both verified), a zero-sprite file, and a linked sprite (verifying the link's own zero length/`LinkedIndex` and that the sprite it links to still decodes correctly); error paths: an out-of-range `LinkedIndex`, a sprite linking to itself, and a failing writer; `ParseV2` against synthetic (byte-built) `.sff` v2 fixtures: header fields (version, sprite/palette counts), the sprite table resolving `(group, image)` pairs to file offsets against either the literal or translated data section depending on the sprite's on-disk flag, a linked sprite (zero pixel-data length, `LinkedIndex` set), the palette table resolving `(group, number)` pairs to file offsets, and a linked palette bank; error paths: a truncated header, a wrong signature, a version byte that doesn't identify a v2 file, and the sprite or palette table cut off before the header's declared count; `DecodeV2Sprite` against synthetic fixtures built with the standard library's own `image/png` encoder (so expected pixel values are independently derived, not a self-check against the decoder): raw (uncompressed) indexed data, PNG8 indexed data, PNG24 RGB data, PNG32 RGBA data (alpha channel verified), and a 1x1 boundary sprite; RLE8-encoded data covering a pure-literal stream, a run mixed with literals, and a single run filling the entire declared image; LZ5-encoded data covering a combined literal-run-plus-packed-back-reference pattern, an extended literal run, a long-distance back-reference, and decoding regardless of the declared color depth (real LZ5 sprites are declared with `ColorDepth == 5`, not 8, unlike every other indexed format); error paths: raw pixel data whose length doesn't match the declared width/height, an unrecognized format code, corrupt/truncated PNG data, a PNG whose own embedded dimensions disagree with the declared sprite size, for RLE8 specifically: data too short to hold its length prefix, a run truncated before its value byte, a run overrunning the declared image size, data that runs out before filling the declared image size, an unsupported color depth, and invalid (zero) declared dimensions; and for LZ5 specifically: data too short to hold its length prefix, data truncated mid-operation, a run overrunning the declared image size, and invalid (zero) declared dimensions; `EncodeV2Sprite` round-tripped through `DecodeV2Sprite` for raw, PNG8, PNG24, and PNG32 pixel data (including a PNG32 sprite with non-opaque alpha) and a 1x1 boundary sprite; error paths: the real but unimplemented `V2FormatRLE8` format code (encode side only — `DecodeV2Sprite` now supports it, `EncodeV2Sprite` does not yet), and a pixel buffer whose `BytesPerPixel` doesn't match what the target format requires; `SerializeV2` round-tripped through `ParseV2`/`DecodeV2Sprite` for a multi-sprite fixture mixing raw and PNG-encoded (PNG8/24/32) sprites plus a palette bank, verifying pixel data, format codes, and the palette bank reference all survive; a zero-sprite/zero-palette file, a linked sprite, and a linked palette bank (both verifying the link's own zero length/`LinkedIndex` and that the target still decodes/reads correctly); error paths: an out-of-range or self-referencing sprite `LinkedIndex`, an out-of-range palette `LinkedIndex`, and a failing writer; `Load` against synthetic v1 and v2 fixtures (built via `SerializeV1`/`SerializeV2`): correct grouping/dimensions/derived `Palette` values for v1 (including a shared-palette sprite and a linked sprite whose dimensions come from its link target), and direct table-to-`Sprite` mapping for v2 (no pixel decoding needed or performed); error paths: a mutual link cycle between two v1 sprites with no pixel data anywhere (must error instead of hanging or panicking), an unrecognized file signature, and a file too short to contain a header; `ResolvePixels` against a hand-built `Palette`: the forced-transparency rule overriding a non-transparent stored value at index 0, the literal rule preserving the palette's own alpha even at index 0, an empty index buffer, and a repeated index resolving to the same color each time; `DecodeV1Palette` against a synthetic 768-byte RGB block with a distinct value per channel (catching a channel mix-up, not just a wrong byte count) and confirming every entry is opaque; error paths: blocks shorter and longer than exactly 768 bytes; `ResolveV1Palette` against a hand-built `V1SpriteTable`: a sprite owning its palette, a shared sprite inheriting its nearest earlier owner, and a chain of multiple shared sprites walking back to the real owner; error paths: no earlier sprite owning a palette, and an out-of-range sprite index; `DecodeV2Palette` against synthetic RGBA color data: a full 256-color bank and a bank declaring fewer than 256 colors (undeclared entries left at their zero value); error paths: a length not a multiple of 4, and more than 256 declared colors; `ResolveV2Palette` against a hand-built `V2SpriteTable`: a bank owning its own color data, a zero-length bank inheriting a linked bank's colors, and a zero-length bank whose `LinkedIndex` is 0 resolving to the first bank; error paths: an out-of-range bank index and a cyclic link chain between two banks; `DecodeExternalPalette` against a synthetic 768-byte `.act` buffer sharing `DecodeV1Palette`'s distinct-per-channel fixture: the file's first/second triplets landing at palette indices 255/254 (reversed order) and its last triplet landing at index 0 with alpha forced to 0 while every other index stays opaque; error paths: buffers shorter than, longer than, and empty versus exactly 768 bytes; `ResolveV1Palette`/`ResolveV2Palette` supplied a non-nil override: returning the override verbatim instead of the sprite's/bank's own palette, including when the supplied index is out of range for the underlying table (proving the override bypasses the lookup rather than merely winning a tie); also verified against real character `.sff`/`.act` files from a local corpus (not committed as fixtures) — resolving a real sprite with and without a real external palette produces different colors, confirming the override changes actual decoded output, not just returning its own input back unchanged |
