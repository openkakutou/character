# Module: root

**Role:** Entry point of the library. Will assemble the format-specific sub-packages (`def`, `sff`, `air`, `cns` — not yet implemented) into a single `Character` struct, the unit a library consumer actually works with.
**Files:** `character.go`
**Exports:** `Character` (struct)
**Depends on:** none yet (sub-packages `def/`, `sff/`, `air/`, `cns/` do not exist yet)
